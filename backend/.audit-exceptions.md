# npm audit known false positives

## drizzle-kit esbuild chain (4 moderate)

npm audit reports vulnerabilities in the range `drizzle-kit@0.19.0 - 1.0.0-beta.1`
via `@esbuild-kit/core-utils` and `esbuild@<=0.24.2`.

**Status: False positive** — drizzle-kit@0.31.10 is installed and no longer 
uses `@esbuild-kit` internally. The npm advisory database range is stale.

No action required.
